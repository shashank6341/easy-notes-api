const router = require('express').Router();

// Require the notesController here

// Create a new Note

// Retrieve all Notes

// Retrieve a Single Note with NoteId

// Update a Note with nodeId

// Delete a Note with its nodeId
